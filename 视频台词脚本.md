# 海普洛斯文库 Pooling 表自动化生成系统 · 视频介绍台词脚本

> 建议时长：约 4 分 40 秒（旁白约 1000 字，正常语速 220 字/分钟）
> 结构：8 个分镜
> 每段标注【画面】= 屏幕上展示什么、【旁白】= 念出来的台词

---

## 分镜 1 · 开场：痛点引入（0:00 — 0:35）

【画面】
屏幕上是一份手工 Excel 表，鼠标在几十行数据间来回点选、复制、粘贴、手改公式。旁边一个计时器在走，数字不断跳动。可以配一点紧张的背景音。

【旁白】
在测序文库的日常工作中，有一件事几乎每个实验员都头疼——做 Pooling 表。
一份表格，要经过查浓度、算取样体积、分组、算汇总、标记纯化、生成定量表……少则七八道、多则十几道工序。
全靠手工，一坐就是一两个小时，还容易算错。

【字幕建议】
「一份 Pooling 表，8~13 道工序，手工 1~2 小时」

---

## 分镜 2 · 项目定位：一句话说清（0:35 — 1:00）

【画面】
屏幕切到项目标题页，依次打出四套表格的名称：PE100、PE150、XP、真迈。

【旁白】
今天要介绍的这个项目，就是来解决这件事的。
它把 PE100、PE150、XP、真迈四种测序文库的 Pooling 表制作，全部做成了自动化。
只要把本批次的源表放进指定目录，说一句话，表格就自动生成好了。

【字幕建议】
「四套管线 · 一键生成 · 全自动化」

---

## 分镜 3 · 系统架构：四层结构（1:00 — 1:40）

【画面】
展示项目目录结构，依次高亮四层：最上层 Skill 入口 → 中间四条流水线 → 共享模块 → 最底层数据目录。

【旁白】
整个系统分四层。
最上面是统一的 Skill 入口，负责听懂你的指令、自动路由到对应型号；
中间是四条独立的流水线，分别对应 PE100、PE150、XP 和真迈；
再往下是共享模块，把找文件、格式化、校验这些通用逻辑抽出来复用；
最底层是输入和输出目录——源表放进去，结果表吐出来。数据不进 Git，安全隔离。

【字幕建议】
「入口 / 管线 / 共享 / 数据 四层架构」

---

## 分镜 4 · 核心流程演示（1:40 — 2:50）

【画面】
以 XP 为例运行流水线，界面滚动输出每一步日志：迁移 → 分组 → 查找 → 公式 → 汇总 → 命名 → 标记 → 稀释 → 定量。

【旁白】
下面看一次真实的运行。以 XP 为例，整套流水线一共九步。
第一步，把上机表的数据迁移进来；
第二步，按 lane 和业务规则分组排序；
第三步，从质检表、自建库表查找补全信息；
第四步，计算每个文库的取样体积，让它们都落在合理的区间；
第五步，生成汇总行、组浓度和补水体积；
第六步，给每个 lane 命名；
第七步，自动标记「纯化」和「已定量」的状态；
第八步，生成稀释计算表；
第九步，生成 PCR 定量表。
每一步都在日志里清清楚楚，最后还有一道自动校验，逐条核对业务规则，有异常会明确提示。

【字幕建议】
「九步流水线，逐步留痕，末尾自动校验」

---

## 分镜 5 · 成果展示：交付物（2:50 — 3:20）

【画面】
打开生成的 Excel，依次展示 Pooling 表的数据面、汇总行、下机数据统计模版，以及单独的 PCR 定量表。

【旁白】
运行结束，得到的是可以直接交付的成果。
Pooling 表里，数据行、汇总行、状态标记、公式全都就位；
下机数据统计模版自动填好了每一条 lane 的信息；
XP 和真迈还会额外产出一份 PCR 定量表。
格式、字体、列宽，都按模板统一设置好了，拿来就能用。

【字幕建议】
「Pooling 表 + 下机统计 + PCR 定量表，交付即用」

---

## 分镜 6 · 两种使用方式（3:20 — 3:50）

【画面】
左边演示在 WorkBuddy 里输入「帮我做 XP 表」；右边演示命令行 `python XP/code/run_all.py`。

【旁白】
使用上非常灵活，两种方式任选。
一种是在 WorkBuddy 里，直接说一句「帮我做 XP 表」，它自己会校验源表、跑完整套流程、把结果交给你；
另一种是命令行，一条命令跑到底。
没有 AI 环境，也照样能用。

【字幕建议】
「AI 对话一句话 · 或命令行一条命令」

---

## 分镜 7 · 量化成效：数据说话（3:50 — 4:20）

【画面】
数据对比图：左边「手工 1~2 小时」被划掉，右边「自动化 1~2 分钟」放大高亮；下方一行「出错率接近零」。

【旁白】
最后看一组数字。
同样的表，手工做要一到两个小时，现在一到两分钟；
十几道工序被压成一条流水线，出错率接近零；
更重要的是，老师傅的经验被固化成了代码——新人也能做出一模一样的结果，经验不再只存在某个人的脑子里。

【字幕建议】
「1~2 小时 → 1~2 分钟 · 出错率接近零 · 经验可传承」

---

## 分镜 8 · 结尾：一句话升华（4:20 — 4:40）

【画面】
回到项目标题，配一句总结文字。

【旁白】
把重复交给机器，把判断留给人。
这就是海普洛斯文库 Pooling 表自动化生成系统。
谢谢观看。

【字幕建议】
「把重复交给机器，把判断留给人」

---

## 附：录制小贴士

| 项目 | 建议 |
|------|------|
| 语速 | 正常偏快，关键数字（9 步、1~2 分钟）稍作停顿强调 |
| 录屏顺序 | 先录好「真实运行」的屏幕片段，再按分镜配音，后期对轴最稳 |
| 演示型号 | 建议用 XP 演示（9 步最完整，还多一张 PCR 定量表，最有说服力） |
| 数据脱敏 | 录屏前先确认源表里的样本编号、客户单位是否需要打码 |
| 补一个彩蛋 | 可在分镜 4 结束后，展示「错误提示」——故意放错源表，让系统明确报出缺哪个文件，体现健壮性 |
