# 2026-08-24 工作日志

## 下机数据统计模版新增 C 列「文库ssDNA编号」
- 需求：PE100/PE150 的下机数据统计模版在 C 列插入新列「文库ssDNA编号」，值取自 T7+制备表 B 列
- 因插入列，原 C~N 列全部后移一列（共 15 列 A~O）
- 模板文件由用户手动改好（两个模板均 15 列，C=文库ssDNA编号）
- 代码改动：
  - `PE_100/code/step6_down_data.py`：列号后移 + 新增 C 列填 ssdna_id + 公式改为 J==I-H、K==J/H + 合并/边框/条件格式列号同步后移
  - `PE_150/code/step7_offline_stats.py`：同上（公式 K==IF(H=0,"",J/H)）
- 验证：PE150 全流程 PASSED，C 列正确填入 B1/B2...，后续列及公式正确；PE100 无源表未实跑（语法通过）
- 未提交 git（用户要求手动提交）

## 格式迁移修正（补）
- 关键发现：下机数据统计模版的数字格式由 `format_font.py` 的 `format_offline_stats`（PE150）硬编码旧列号设置，插入列后需迁移
- PE150 `format_font.py` format_offline_stats：两位小数 (7,8,9)→(8,9,10)、整数 col4→col5、百分比 col10→col11、框线 range(1,15)→(1,16)、条件格式 I/J→J/K
- PE150 `step7_offline_stats.py`：撤回误加的 number_format（格式职责归 format_font）
- PE100 格式在 step6_down_data.py 内直接设置（已迁移），format_font 不碰下机表
- 遗留：common/format_utils.py 的 apply_standard_format 会对所有 sheet（含下机表）按列字母套 number_formats，导致下机表 F 列（文库类型）被设 '0.000'，但文本列无实际影响，暂未修

## 下机表逻辑统一（PE100/PE150，5项）
- 需求：统一两个项目下机表逻辑
- 改动：
  1. PE100 补测率公式加除零保护 `=IF(H=0,"",J/H)`（step6）
  2. PE100 文库总数匹配改 laneID 枚举 `f'{sn}{gi}'`（跟 PE150 一致）
  3. A 列都按 lane 合并、L 列都全表合并（PE100 A 改按lane；PE150 L 改全表）
  4. PE150 数字格式移到 step7（write_cell 加 number_format 参数；format_font 的 format_offline_stats 移除数字格式）
  5. PE150 上机模式值 'PE150' → 'T7+150'
- 附带：common/format_utils.py 的 apply_standard_format 跳过「下机数据统计模版」的 number_formats（避免覆盖 step 里设置的格式）
- 验证：两项目均 PASSED，格式/合并/公式/上机模式值全部一致正确
- 未提交 git（用户手动提交）

## L 列浅绿色背景
- 需求：PE100/PE150 下机表 L 列（上机模式）数据行填充浅绿色（FF92D04F，与模板 I/J 列表头同色）
- 改动：PE100 step6 加 LIGHT_GREEN 并给 L 列设 fill；PE150 step7 加 LIGHT_GREEN + write_cell 加 fill 参数
- 验证：L2 填充 FF92D04F 正确（L 列全表合并，合并区统一显示浅绿色）

## PE100 下机表字体 + 列宽修复
- 问题：PE100 下机表字体不是 Times New Roman，且列宽太短（内容缩进单元格）
- 根因：PE100 run_all.py 执行顺序为 format_font 先、step6 后，format_font 的 auto_column_width/字体在 step6 填数据前执行，基于空表计算
- 修复：调整顺序为 step6 先、format_font 后（与 PE150 一致），字体和列宽在填数据后正确设置
- 验证：PE100 字体 Times New Roman 10pt、列宽正确

## 四项目操作文档更新（按最新代码同步）
- 更新 PE100/PE150/XP 三个操作文档，ZM 核对后无需改
- PE100：执行顺序（step6先/format后）、下机表列结构 A-N→A-O（新增D列ssDNA编号）、T7 Q列 =L/I、输出确认第6条
- PE150：T7 Q列 =L/I、下机表列结构 A-N→A-O、数字格式移到step7、上机模式 T7+150、format_font 下机表专属格式删数字格式
- XP：step7_mark 标记规则改为"单文库组+P有值→已定量无条件优先"
- 提醒用户：ZM 的 step7_mark 代码仍是旧逻辑，未同步 XP

## ZM 标记逻辑同步 XP（代码+文档）
- 用户确认后，同步 ZM 的 step7_mark.py + validate_output.py 为 XP 新逻辑（单文库组+P有值→已定量无条件优先）
- ZM 文档 step7_mark 标记规则同步更新
- 验证：ZM 全流程 PASSED（本批已定量0组，纯化 S面4组/P面11组）

## 发现：仓库体积 37MB，根因是早期提交把源表写进了 git 历史
- 用户从 GitHub clone 到 E:\hpls-table-maker，发现 38MB；工作文件仅 ~1.4MB，.git 占 36MB
- 历史大 blob：三个「外包文库质检总表2026.xlsx」各 ~8.4MB、XP 纯化总表 4.6MB、自建库出库报告 ~1.9MB×3
- 引入提交：44e83cf（初始化）、fee9480；4533fbf 才移除追踪并加 .gitignore
- 当前 HEAD 已不追踪 input_data，但历史 blob 永久留存 → 客户敏感数据仍可被任何人从 GitHub 历史取出
- 待用户决策：是否用 git filter-repo 清理历史 + force push（或改私有仓库/重建仓库）；未擅自执行

## 执行历史清理（用户决定只清历史、不改私有）
- 备份：`E:\HP_Project_git_backup_20260824_111218`（37MB 完整 .git）
- 先尝试 git filter-repo（下载单文件到 E:\_git_tools）：清理成功但 **repack 阶段 .git/objects 全丢**，HEAD 变 bad object
  - 根因：.git 被其他进程锁定（`mv .git` 报 Permission denied 证实），本环境 git 对象重写已连续失败两次（上次 rebase 同样丢 refs+objects）
  - 工作区文件全程完好（filter-repo 不动工作区）
- 改用**重建仓库**方案（更可靠且绕开该 bug）：
  1. 删 .git 内坏引用（packed-refs/refs/logs/index/ORIG_HEAD/FETCH_HEAD）→ `git init` 原地重初始化
  2. 重加 remote（SSH）→ `git add -A` → 严格检查暂存内容（93 文件、6 模板表在、input_data 源表被 .gitignore 正确忽略）
  3. 单次提交 `21d02eb` → `git push -f origin master` 成功
- 结果：.git 37MB → **1.2MB**；远程只剩 1 个提交；正常 clone/浏览已无任何客户源表
- **残留风险（已实证）**：旧提交 44e83cf 作为悬空对象仍在 GitHub 服务端，
  `api.github.com/.../contents/PE_100/input_data?ref=44e83cf` 仍能列出并下载 8.8MB 质检总表
  → 彻底清除只有三条路：改 Private / 联系 GitHub Support 清缓存 / **删仓重建同名仓库**（推荐，用户自己可做）
- 本地遗留待用户处置：备份 37MB、`E:\_git_tools` 208K、旧 clone `E:\hpls-table-maker` 38MB（历史已过时，需重新 clone）

## 经验教训（重要）
- 本机 E: 盘 .git 对象重写操作（rebase/filter-repo repack）会因文件锁失败并丢对象 → **凡涉及历史重写，一律先完整备份 .git，并优先考虑"重建仓库"而非原地重写**

## 删库重建（彻底清除悬空对象）— 已完成
- 因 force push 后旧提交仍以悬空对象存在于 GitHub 服务端（实测可下载 8.8MB 质检总表），用户决定删库重建
- 无 gh CLI / GITHUB_TOKEN，SSH key 只能做 git 操作不能管理仓库 → 由用户在网页手动删除并新建同名空仓库
- 用户建好后执行 `git push -u origin master`（remote 地址不变，SSH 免密）
- **验收结果（逐个实测 GitHub API）**：
  - 旧提交 44e83cf / fee9480 / 4533fbf / 07da099 → 全部 **HTTP 422（不存在）** ✅
  - 新提交 21d02eb / 2fec18c → HTTP 200 ✅
  - 远程文件树：93 文件 = 67 py + 11 md + 6 模板表（PE100 模板 704KB 在）
  - input_data/output_result 下非 .gitignore 文件数 = **0** ✅
- 结论：客户源表已从 GitHub 彻底不可访问；仓库仍为 public（用户未改私有）
- 本地遗留待处置：`E:\HP_Project_git_backup_20260824_111218`(37MB 含敏感历史)、`E:\hpls-table-maker`(已重新 clone，见下)、`E:\_git_tools`(已清理)

## 重新 clone 验证（E:\hpls-table-maker）
- 用户 11:38 重新 clone，体积 38MB → **2.4MB**（.git 976KB），3 个新提交
- 校验结果：93 文件数一致、无缺失无多余；忽略 CRLF/LF 后**内容 0 差异**；6 个 xlsx 模板表**二进制完全相同**
- 全部 py 文件 compileall 通过；缺源表时报错清晰（`input_data/ 中没有包含 "上机" 的 xlsx 文件`）
- skills 无硬编码绝对路径，可移植
- ⚠️ **当前存在两个副本**：`E:\HP_Project`（主工作目录，SSH remote，含 input_data 源表）
  与 `E:\hpls-table-maker`（clone 验证用，HTTPS remote，input_data 为空）
  → 约定：**日常跑表只用 E:\HP_Project**，避免两处改代码造成分叉

## 项目体积分析（E:\HP_Project 共 60MB）
- 构成：input_data 源表 **54.8MB**（占 91%）、.git 1.2MB、代码+文档+模板约 2.5MB、output_result 约 0.9MB
- **重要发现：四个项目 input_data 里的同名源表 hash 全不相同**，是各自跑表当期放入的版本快照：
  - 「外包文库质检总表2026.xlsx」4 份：PE_100(8/6, 8.45MB)、PE_150(8/4, 8.36MB)、XP(8/12, 9.00MB)、ZM(8/6, 8.47MB) → 重复占 25.3MB
  - 「2026年纯化总表.xlsx」2 份：XP(8/12, 4.78MB)、ZM(8/6, 4.56MB) → 重复 4.6MB
  - 「自建库出库报告」PE_100/PE_150 各 1 份（文件名有笔误 `.xlsx.xlsx`）、XP/ZM 各 1 份 → 重复 3.6MB
  - 合计冗余约 33.5MB
- ⚠️ 结论：**不能假设各项目源表内容一致**，也不能盲目合并去重（会改变历史批次的数据基准）
- 60MB 属正常本地数据，不影响 Git（1.2MB）/ GitHub / 同事 clone

## 🚨 重大发现：PE100 模板表内嵌外部链接，泄露 2024 年客户样本数据
- `PE_100/PE100_pooling模板表.xlsx` 704KB（其他 5 个模板仅 17~43KB），根因是内嵌 `xl/externalLinks/`
  - externalLink1.xml：4.3MB，缓存 **100,203 个单元格值**
  - externalLink2.xml：254KB，缓存 5,518 个值
- 泄露内容（实测抽样）：样本编号 `HGC-POOL-2449785`、样本名 `H1-9YCKVNM-HUMAN-D1`、
  检测人 `YHT`、浓度、平均片段；"华大" 出现 54 次
- 泄露内网路径：`C:\hgc\生物技术中心\质控上机组\外包文库质检总表2024.xlsx`
  与 `...\DNBSeq-T7\202501\202250117\江西Xplus上机-20240802.xlsx`
- 依赖情况：模板 sheet4（名为 **B1-B3**）有 **69 处** VLOOKUP 引用外部链接（如 `VLOOKUP(B2,'[1]7月份'!$E:$N,6,0)`）
  → 属历史遗留废公式：**输出表中引用外部链接的公式数 = 0**（跑表时被 clear_pooling 清空）
- ⚠️ 但输出表仍携带 4 个 externalLink 文件 → **每份交付的结果表都夹带这 4.5MB 旧样本数据**
- 其他 5 个模板（PE150/XP×2/ZM×2）扫描结果：**无外部链接，干净**
- 处理方案（待用户确认）：移除 externalLinks + 清掉 B1-B3 的 69 个废公式 → 模板可从 704KB 降到约 20KB

## PE100 模板清理 — 已执行完成
- 备份目录 `E:\HP_backup_20260824\`：原模板、清理前基准输出表、清理脚本 `clean_external_links.py`
- 关键依据：`clear_pooling.py` 对**所有 sheet** 执行 `delete_rows(2, max_row-1)`（只留表头）
  → 模板里任何数据/公式残留都会被清空，因此预先清空完全安全，模板本该只有表头
- 清理两步：
  1. zipfile 精确重建：删 `xl/externalLinks/**` + `calcChain.xml`，改 workbook.xml（去 externalReferences）、
     workbook.xml.rels（去 2 个关系）、[Content_Types].xml（去 2 个 Override）、sheet4.xml（去 69 个外链公式）
     → 704KB → 18KB，除必改 4 个文件外其余 zip 条目字节完全一致
  2. openpyxl 清空各 sheet 第 2 行起（T7+制备 25 行→1 行、B1-B3 37 行→1 行）→ 18KB → **10.4KB**
- **验收（决定性对比）**：跑 PE100 全流程 PASSED、26 组
  - 逐单元格比对 3544 个：**值差异 0、数字格式差异 0**
  - sheet 列表一致、合并单元格全部 IDENTICAL
  - 输出表 631KB → **31KB**；externalLink **无**；2024 年残留编号全部 NOT FOUND；内网路径 NOT FOUND
  - 输出表 122 个 HGC 编号均为本批 `HGC-Lib-26092xxx`（2026 年），数据正确
- 结论：模板 704KB→10.4KB，输出表 631KB→31KB，客户数据与内网路径彻底清除，业务输出零变化

## clone 体积实测 + 发现新的历史残留
- 实测 `git clone`：下载 **900.42 KiB**，clone 后目录 1.8MB，GitHub 报告 size=887KB → 体积对新用户完全无压力
- ⚠️ **但历史里仍有含客户数据的旧模板**：
  - `PE_100/PE100_pooling模板表.xlsx` 在历史中有两个版本：
    `546cf43` → 10,634 字节（干净）、`21d02eb` → 720,867 字节（**含 4 个 externalLink + HGC 编号 + 内网路径**）
  - 实测 `git show 21d02eb:PE_100/PE100_pooling模板表.xlsx` 可完整取出含客户数据的旧模板
- 当前仓库共 7 个提交（21d02eb 初始 + 6 个记录/清理提交）
- 待用户决策：再做一次删库重建（历史短、成本低）以彻底清除；或接受现状

## 其他代码冗余（设计层面，非垃圾文件）
- XP/code 与 ZM/code 各 17 个 py，其中 **10 个内容完全相同**（pooling_utils、source_lookups、
  step2_group_sort、step3_lookup、step4_formula、step5_summary、step6_lane_name、step7_mark、
  step8_dilution、validate_output）→ 当初为保持独立性未抽共享模块，重构风险大于收益
- PE_100/code 与 PE_150/code 仅 clear_pooling.py 相同，其余因业务差异不同，属正常
