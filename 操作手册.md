# HPLS 文库表生成系统 — 操作手册

本系统用于自动化生成 PE100、PE150、XP、真迈四种测序文库 Pooling 表。通过 WorkBuddy 与 AI 对话即可操作，无需编程知识。

---

## 一、准备工作（首次使用）

### 1. 安装 WorkBuddy

下载地址：https://www.workbuddy.cn（如已有请跳过）

### 2. 拿代码

打开 WorkBuddy，在左侧找到"终端"，输入以下命令：

```bash
git clone https://github.com/www0922/hpls-table-maker.git
```

执行后在本地会生成一个 `hpls-table-maker` 文件夹。

### 3. 打开项目

在 WorkBuddy 中打开克隆下来的 `hpls-table-maker` 文件夹。

---

## 二、每次使用流程（三步走）

### 第一步：放入输入文件

根据你要生成的表型，把对应的 Excel 源表放进相应的 `input_data` 文件夹：

**打开文件管理器**，进入项目目录，找到对应文件夹：

| 要做哪种表 | 把文件放进 | 需要哪些文件（文件名包含以下关键词） |
|-----------|-----------|----------------------------------|
| PE100 | `PE_100\input_data\` | `上机`、`质检`、`自建库` |
| PE150 | `PE_150\input_data\` | `上机`、`质检`、`自建库` |
| XP | `XP\input_data\` | `Xplus上机`、`质检`、`自建库`、`纯化`、`qPCR` |
| 真迈 | `ZM\input_data\` | `江西上机`、`质检`、`自建库`、`纯化`、`qPCR` |

> **⚠️ 注意：**
> - 每个关键词必须恰好对应**一个**文件，多一个少一个都会报错。
> - 只需文件名里**包含**关键词即可，比如 "江西Xplus上机-20260811.xlsx" 包含 "Xplus上机"，可以匹配。
> - 同个文件夹只保留**当前批次**的文件，旧批次请移走。

**举例：做 PE100 表**

```
PE_100\input_data\
├── 江西华大T7+上机-PE100-20260811.xlsx   ← 包含"上机"
├── 外包文库质检总表2026.xlsx            ← 包含"质检"
└── 2026年1月-12月自建库出库报告.xlsx     ← 包含"自建库"
```

### 第二步：跟 AI 对话

在 WorkBuddy 的输入框里，直接说一句人话即可：

```
帮我做 PE100 表
```

或

```
帮我做 PE150 表
帮我做 XP 表
帮我做真迈表
```

AI 会自动检查文件是否齐全，齐全后自动运行，你什么都不用管。

### 第三步：取走结果

运行完成后，输出文件在对应型号的 `output_result` 文件夹里：

| 表型 | 输出位置 | 输出文件 |
|------|---------|---------|
| PE100 | `PE_100\output_result\` | `20260811文库pooling表T7+PE100.xlsx` |
| PE150 | `PE_150\output_result\` | `20260811文库pooling表T7+PE150.xlsx` |
| XP | `XP\output_result\` | `20260811文库pooling表.xlsx` + `20260811PCR定量表.xlsx` |
| 真迈 | `ZM\output_result\` | `20260811文库pooling表AE0.xlsx` + `20260811PCR定量表真迈.xlsx` |

> 文件名中的日期（如 `20260811`）为运行当天的日期，每次自动生成。

---

## 三、常见问题

### Q: 运行报错"缺少文件名包含 X 的 xlsx 文件"怎么办？

A: 检查对应 `input_data` 文件夹，确保有且只有一个文件包含那个关键词。多放或少放都会报错。

### Q: 运行报错"关键词匹配到多个文件"怎么办？

A: 同一个关键词（比如"质检"）匹配到了多个文件。删除多余文件，只保留当前批次的那一个。

### Q: 今天跑过了，想再跑一次会怎样？

A: 会提示输出文件已存在，并询问是否覆盖。确认后即可重新生成。

### Q: 可以用自己命名的文件夹作为输入/输出吗？

A: 可以。跟 AI 说"用 D:\今天的数据 做 PE100 表"，AI 会按要求使用指定目录。不指定则默认用项目内的 `input_data` 和 `output_result`。

### Q: 电脑没装 Python 要紧吗？

A: 不要紧。WorkBuddy 自带 Python，首次运行时自动安装所需依赖，全程无需额外操作。

### Q: 后续有更新怎么获取？

A: 在 WorkBuddy 的终端中输入：

```bash
git pull
```

即可获取最新版本。

---

## 四、文件结构参考

```
hpls-table-maker/
├── PE_100/              ← PE100 项目
│   ├── input_data/      ← 放源表（每批次替换）
│   └── output_result/   ← 生成结果在这里
├── PE_150/              ← PE150 项目
│   ├── input_data/
│   └── output_result/
├── XP/                  ← XP 项目
│   ├── input_data/
│   └── output_result/
├── ZM/                  ← 真迈项目
│   ├── input_data/
│   └── output_result/
└── README.md            ← 本文件
```

---

## 五、与 AI 对话示例

```
你：帮我做 PE100 表
AI：[检查] 找到上机、质检、自建库各一个文件，开始生成...
AI：[完成] 文件已生成在 PE_100\output_result\

你：帮我做真迈表
AI：[检查] 缺少质检文件，请放入 ZM\input_data\ 后再试。
你：放好了，重新跑
AI：[完成] 文件已生成在 ZM\output_result\
```
